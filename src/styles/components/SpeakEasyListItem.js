import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  component: {
    borderWidth: 1,
    flex: 1,
    padding: 20,
  },
  name: {
    textAlign: 'center',
    fontSize: 20
  },
  tags: {
    textAlign: 'center',
    fontSize: 15
  }
});

export default styles
