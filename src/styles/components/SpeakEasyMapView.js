import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  view: {
    flex: 4,
    alignItems: 'stretch',
  },
  map: {
    flex: 1,
    borderWidth: 1,
    borderColor: '#000000',
  },
  chosen: {
    flex: 1,
    height: 10,
  }
});

export default styles;
