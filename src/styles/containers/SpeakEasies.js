import { StyleSheet } from "react-native";
import variables from "../variables";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: variables.navHeight,
    backgroundColor: variables.backgroundColor,
  },
});

export default styles;
